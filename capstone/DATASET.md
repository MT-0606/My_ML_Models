# Dataset

The complete CSV dataset is stored as GitHub Release assets because it exceeds GitHub's normal repository file-size limit.

Download the file (`profiles.csv`) from the repository's **OKCupid Dataset** release and place it in this directory before running the notebook.
